# Documentation-only schema. This file is intentionally not a deployable
# Kubernetes Secret.
secret_contract:
  api_version: v1
  resource_kind: Secret
  metadata:
    name: glasslab-v2-postgres
    namespace: glasslab-v2
  required_keys:
    - POSTGRES_DB
    - POSTGRES_USER
    - POSTGRES_PASSWORD
  example_values_are_not_deployable: true
