apiVersion: apps/v1
kind: Deployment
metadata:
  name: glasslab-intake-agent
  namespace: glasslab-v2
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: glasslab-intake-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: glasslab-intake-agent
    spec:
      imagePullSecrets:
        - name: glasslab-ghcr-pull
      containers:
        - name: intake-agent
          image: ghcr.io/ccny-glasslab/glasslab-intake-agent:0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: GLASSLAB_INTAKE_AGENT_PROVIDER_API
              value: openai-compatible
            - name: GLASSLAB_INTAKE_AGENT_PROVIDER_BASE_URL
              value: http://192.168.1.21:52415
            - name: GLASSLAB_INTAKE_AGENT_MODEL
              value: mlx-community/Qwen3-Coder-Next-4bit
            - name: GLASSLAB_INTAKE_AGENT_TIMEOUT_SECONDS
              value: "120"
          ports:
            - containerPort: 8090
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
