apiVersion: batch/v1
kind: Job
metadata:
  name: metric-search-smoke-test
  namespace: glasslab-v2
spec:
  template:
    spec:
      containers:
        - name: metric-search
          image: ghcr.io/ccny-glasslab/glasslab-metric-search:smoke-test-81a02667108d5924e000d89d3e2e63dccd789755
          env:
            - name: GLASSLAB_RUNNER_EXPERIMENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GLASSLAB_IMAGE_COMMIT
              value: "smoke-test-81a02667108d5924e000d89d3e2e63dccd789755"
          command: ["bash", "-c"]
          args:
            - |
              cd /app
              python3 scripts/run_experiment.py \
                --config configs/search_spaces/cifar100_contrastive_v0.yaml \
                --output-dir /mnt/artifacts/smoke-test-run-${GLASSLAB_RUNNER_EXPERIMENT_ID} \
                --epochs 1 \
                --max-train-batches 1 \
                --max-eval-batches 8 \
                --backbone resnet18 \
                --loss contrastive
              echo "Output dir: /mnt/artifacts/smoke-test-run-${GLASSLAB_RUNNER_EXPERIMENT_ID}"
              ls -lh "/mnt/artifacts/smoke-test-run-${GLASSLAB_RUNNER_EXPERIMENT_ID}" || true
              test -f "/mnt/artifacts/smoke-test-run-${GLASSLAB_RUNNER_EXPERIMENT_ID}/metrics.json" && echo "metrics.json found" || echo "ERROR: metrics.json not found"
          resources:
            limits:
              nvidia.com/gpu: 1
              memory: "8Gi"
              cpu: "2"
            requests:
              memory: "4Gi"
              cpu: "1"
          volumeMounts:
            - name: artifacts
              mountPath: /mnt/artifacts
            - name: datasets
              mountPath: /mnt/datasets
      restartPolicy: Never
      nodeSelector:
        glasslab.io/gpu-vendor: nvidia
      imagePullSecrets:
        - name: glasslab-ghcr-pull
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: glasslab-shared-artifacts
        - name: datasets
          persistentVolumeClaim:
            claimName: glasslab-shared-datasets
  backoffLimit: 1
  ttlSecondsAfterFinished: 86400
