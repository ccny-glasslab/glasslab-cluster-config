apiVersion: batch/v1
kind: Job
metadata:
  name: baseline-clip
  namespace: glasslab-v2
  labels:
    app: glasslab-metric-search
    baseline-type: clip
spec:
  template:
    spec:
      containers:
        - name: baseline
          image: ghcr.io/ccny-glasslab/glasslab-metric-search:03f7062935280c6fd7c6d6598a460d6fb885824b
          env:
            - name: GLASSLAB_RUNNER_EXPERIMENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GLASSLAB_IMAGE_COMMIT
              value: "03f7062935280c6fd7c6d6598a460d6fb885824b"
          command: ["bash", "-c"]
          args:
            - |
              cd /app
              python3 /app/scripts/run_baseline.py \
                --baseline=clip \
                --output-dir=/mnt/artifacts/baselines/clip/${GLASSLAB_RUNNER_EXPERIMENT_ID} \
                --max-eval-batches=20 \
                --run-shuffled
              echo "Output dir: /mnt/artifacts/baselines/clip/${GLASSLAB_RUNNER_EXPERIMENT_ID}"
              ls -lh "/mnt/artifacts/baselines/clip/${GLASSLAB_RUNNER_EXPERIMENT_ID}" || true
              test -f "/mnt/artifacts/baselines/clip/${GLASSLAB_RUNNER_EXPERIMENT_ID}/metrics.json" && echo "metrics.json found" || echo "ERROR: metrics.json not found"
          resources:
            limits:
              nvidia.com/gpu: 1
              memory: "32Gi"
              cpu: "4"
            requests:
              memory: "16Gi"
              cpu: "2"
          volumeMounts:
            - name: artifacts
              mountPath: /mnt/artifacts
            - name: datasets
              mountPath: /mnt/datasets
      restartPolicy: Never
      imagePullSecrets:
        - name: ghcr-secret
      nodeSelector:
        glasslab.io/gpu-vendor: nvidia
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: glasslab-shared-artifacts
        - name: datasets
          persistentVolumeClaim:
            claimName: glasslab-shared-datasets
  backoffLimit: 3
  ttlSecondsAfterFinished: 86400
